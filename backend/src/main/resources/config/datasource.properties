# ============================================================
# As-You-Cost — Supabase Cloud PostgreSQL DataSource 설정
# ============================================================
# 01_architecture.md 5계층 아키텍처 기준
# Data Layer: Supabase Cloud PostgreSQL (PgBouncer Pooler)
# ============================================================

# ── JDBC 기본 접속 정보 (Supabase) ──
datasource.driver-class-name=org.postgresql.Driver
datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require&prepareThreshold=0&currentSchema=asyoucost&stringtype=unspecified
datasource.username=postgres.zxenhxqhfglorxgibmrz
datasource.password=admin1004!@#$

# ── HikariCP Connection Pool ──
# Supabase 무료 플랜 연결 제한 고려 (최대 60 direct / pooler 200)
datasource.hikari.maximum-pool-size=10
datasource.hikari.minimum-idle=2
datasource.hikari.idle-timeout=180000
datasource.hikari.max-lifetime=600000
datasource.hikari.connection-timeout=30000
datasource.hikari.pool-name=AsYouCostHikariPool

# ── MyBatis 설정 ──
mybatis.mapper-locations=classpath:mapper/**/*.xml
mybatis.config-location=classpath:config/mybatis-config.xml
mybatis.type-aliases-package=smartcost.app.bp
